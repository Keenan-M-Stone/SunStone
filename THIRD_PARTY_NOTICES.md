# Third-Party Notices (SunStone)

SunStone depends on third-party open-source packages.

## JavaScript/Node dependencies
- See `frontend/package.json` and `frontend/package-lock.json` for the dependency list.
- Each dependency remains under its own license.

## Python dependencies
- See `backend/pyproject.toml` for declared dependencies.
- Installed packages include their license metadata and/or `LICENSE` files in their distributions.

## Vendored / copied code
If any source code is copied or vendored into this repository (not via package managers), its original license text and attribution should be included alongside the copied files.
